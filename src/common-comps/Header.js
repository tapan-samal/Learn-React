import React from 'react'

const Header = () => {
  return (
    <h1 style={{height: "40px", backgroundColor: "blue", color: "white", textAlign: "center" }}>I am Header !!</h1>

  )
};

export default Header;